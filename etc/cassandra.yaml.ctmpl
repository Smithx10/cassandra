cluster_name: 'Cassandra-Cluster'
num_tokens: 256 {{ $hostname := env "HOST" }}{{ range nodes }}{{ if eq .Node $hostname }}{{ if service "cassandra-seed" }}{{ range service "cassandra-seed" }}
seed_provider:
  - seeds: {{.Address | .join "," }}{{ end }}{{ else }}
seed_provider:
  - seeds: {{ .Address | .join "," }}{{ end }}
listen_address: {{ .Address }}
rpc_address: {{ .Address }}{{ end }}
endpoint_snitch: GossipingPropertyFileSnitch
